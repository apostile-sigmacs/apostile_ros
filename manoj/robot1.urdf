<?xml version ="1.0"

   < model name = "robot1" >
   < static > true < / static >

   < link name = 'chassis' >
      < pose > 0 0 .1 0 0 0 < / pose >

      < collision name = 'collision' >
         < geometry >
            < box >
               < size > 0.4 0.2 0.1 < / size > 
            < / box >
         < / geometry >
      < / collision >

      < visual name = 'visual'>
         < geometry >
            < box>
               <size> 0.4 0.2 0.1 < / size >
            < / box >
         < / geometry >
      < / visual >

      < collision  name = 'caster collision' >
         < / pose >
   < / link >
   < / model >
   




       

